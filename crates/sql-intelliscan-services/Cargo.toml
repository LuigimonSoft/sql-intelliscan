[package]
name = "sql-intelliscan-services"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
futures = "0.3"
